import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'payment-btn-angular-demo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
